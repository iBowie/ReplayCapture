// --- Timing (QPC + high-resolution waitable timer for the CFR pacer) ---
QueryPerformanceCounter
QueryPerformanceFrequency
CreateWaitableTimerExW
SetWaitableTimerEx
CREATE_WAITABLE_TIMER_HIGH_RESOLUTION
SYNCHRONIZATION_ACCESS_RIGHTS
WaitForSingleObject
CloseHandle
CreateEventW
SetEvent

// --- Display enumeration ---
EnumDisplayMonitors
GetMonitorInfoW
MONITORINFOEXW
EnumDisplaySettingsW
MonitorFromWindow

// --- Audio: WASAPI core ---
// IMMDeviceEnumerator, IMMDevice, IMMDeviceCollection, IMMNotificationClient, IAudioClient,
// IAudioCaptureClient, IAudioRenderClient, IPropertyStore, IAudioSessionManager2,
// IAudioSessionEnumerator and IAudioSessionControl2 are hand-declared in
// Audio/Interop/WasapiInterop.cs as [GeneratedComInterface]s instead of being generated here —
// CsWin32 only emits classic [ComImport]-based COM interop, which Native AOT does not support.
// Their enum/struct parameters (EDataFlow, DEVICE_STATE, CLSCTX, STGM, AUDCLNT_SHAREMODE,
// PROPERTYKEY, PCWSTR) are likewise hand-declared as local mirror types in WasapiInterop.cs, cast
// to/from these CsWin32 types at each call site — empirically, ComInterfaceGenerator cannot
// validate blittability for a type produced by a different source generator in the same
// compilation (SYSLIB1051), even though an identical locally-declared type works fine. PROPVARIANT
// stays CsWin32-generated (via a local mirror layout bridged with Unsafe.As) since PropVariantClear
// — plain CsWin32 P/Invoke, not COM interop — needs the real type.
EDataFlow
ERole
WAVEFORMATEX
WAVEFORMATEXTENSIBLE
AUDCLNT_STREAMFLAGS_LOOPBACK
AUDCLNT_STREAMFLAGS_EVENTCALLBACK
AUDCLNT_STREAMFLAGS_AUTOCONVERTPCM
AUDCLNT_STREAMFLAGS_SRC_DEFAULT_QUALITY
_AUDCLNT_BUFFERFLAGS
PropVariantClear
CoTaskMemFree
PROPVARIANT

// --- Audio: per-process loopback (Windows 10 20348+) ---
// ActivateAudioInterfaceAsync, IActivateAudioInterfaceCompletionHandler and
// IActivateAudioInterfaceAsyncOperation are hand-declared in ProcessLoopbackSource.cs as
// [GeneratedComInterface]s instead of being generated here — CsWin32 only emits classic
// [ComImport]-based COM interop, which Native AOT does not support.
